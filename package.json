{
  "name": "kor-nil-site",
  "version": "1.0.0",
  "description": "Сайт-визитка Алексея Корнилова (kor-nil.ru): Full-stack разработка + SEO",
  "private": true,
  "scripts": {
    "dev": "npx @11ty/eleventy --serve --port=8080",
    "build": "npx @11ty/eleventy",
    "clean": "rm -rf dist",
    "resume": "bash scripts/build-resume.sh",
    "deploy": "npm run build && rsync -rlqz --no-perms -e \"ssh -o BatchMode=yes -o ConnectTimeout=15\" --delete-after dist/ root@sntchat.ru:/root/kor-nil-site/ && bash scripts/indexnow.sh"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "clean-css": "^5.3.3",
    "puppeteer-core": "^24.43.1",
    "sharp": "^0.35.4"
  }
}
